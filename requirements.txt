# Core
diffprivlib>=0.6.0
# scikit-learn is pinned below 1.9 for diffprivlib compatibility (Appendix A of the technical
# report); diffprivlib itself needs >=1.4.0. Tested: 1.6.1 and 1.8.x.
scikit-learn>=1.4.0,<1.9
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0


# LLM backends. All three are needed to reproduce the paper's arms: src/llm_generator.py imports
# `anthropic`, `openai` and `google.genai`. Floors follow the API surface the code uses
# (reasoning_effort; ThinkingConfig on the google-genai client). Tested: 0.120.2 / 3.8.0 / 2.22.0.
anthropic>=0.25.0
openai>=1.60.0
google-genai>=1.0.0
httpx>=0.24.0

# Plotting
matplotlib>=3.7.0
seaborn>=0.12.0

# Paper build (paper/build_pdf.py): python-markdown and the weasyprint command
markdown>=3.5
weasyprint>=60

# Testing
pytest>=7.4.0
